// Stub types — will be replaced when running: pnpm generate-api-types
// Requires backend running at localhost:8000 to generate real types.

export type paths = Record<string, never>;
export type components = Record<string, never>;
